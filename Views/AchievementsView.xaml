<UserControl x:Class="EasyLingo.Views.AchievementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FF030912"/>
            <GradientStop Color="#FF132735" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <TextBlock Text="Osiągnięcia"
                   FontSize="32"
                   FontWeight="SemiBold"
                   Foreground="#FFA8E5FC"
                   Margin="50,20,0,20"/>

        <!-- Główna ramka -->
        <Border Grid.Row="1"
                Background="#FF132735"
                CornerRadius="20"
                Margin="50,0,50,50"
                Padding="20">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- GÓRNA SEKCJA: statystyki -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Kafelek: ukończone zestawy -->
                    <Border Grid.Column="0"
                            Background="#FF0D1E29"
                            CornerRadius="20"
                            Margin="0,0,10,0"
                            Padding="18"
                            Height="120">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Ukończone zestawy"
                                       Foreground="#FFA8E5FC"
                                       FontSize="16"
                                       FontWeight="SemiBold"/>

                            <TextBlock Grid.Row="1"
                                       Text="{Binding CompletedSetsText, Mode=OneWay}"
                                       Foreground="White"
                                       FontSize="36"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Kafelek: ogólny postęp -->
                    <Border Grid.Column="1"
                            Background="#FF0D1E29"
                            CornerRadius="20"
                            Margin="10,0,0,0"
                            Padding="18"
                            Height="120">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Ogólny postęp"
                                       Foreground="#FFA8E5FC"
                                       FontSize="16"
                                       FontWeight="SemiBold"/>

                            <TextBlock Grid.Row="1"
                                       Margin="0,8,0,10"
                                       Text="{Binding OverallProgressPercent, Mode=OneWay, StringFormat={}{0}%}"
                                       Foreground="White"
                                       FontSize="28"
                                       FontWeight="Bold"/>

                            <ProgressBar Grid.Row="2"
                                         Height="10"
                                         Maximum="100"
                                         Value="{Binding OverallProgressPercent, Mode=OneWay}"
                                         Background="#FF132735"
                                         Foreground="#FFA8E5FB"
                                         VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- LISTA OSIĄGNIĘĆ -->
                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">

                    <ItemsControl ItemsSource="{Binding Achievements}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FF0D1E29"
                                        CornerRadius="18"
                                        Margin="0,0,0,12"
                                        Padding="16">

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="42"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Ikonka / status -->
                                        <Border Width="34"
                                                Height="34"
                                                CornerRadius="17"
                                                Background="#FF132735"
                                                VerticalAlignment="Top">
                                            <TextBlock Text="{Binding Icon}"
                                                       Foreground="#FFA8E5FC"
                                                       FontSize="16"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" Margin="12,0,12,0">
                                            <TextBlock Text="{Binding Title}"
                                                       Foreground="White"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"/>

                                            <TextBlock Text="{Binding Description}"
                                                       Foreground="#FFB9DDF2"
                                                       Opacity="0.85"
                                                       FontSize="13"
                                                       TextWrapping="Wrap"
                                                       Margin="0,4,0,10"/>

                                            <ProgressBar Height="8"
                                                         Maximum="100"
                                                         Value="{Binding ProgressPercent, Mode=OneWay}"
                                                         Background="#FF132735"
                                                         Foreground="#FFA8E5FB"/>

                                            <TextBlock Text="{Binding ProgressPercent, Mode=OneWay, StringFormat={}{0}%}"
                                                       Foreground="White"
                                                       FontSize="12"
                                                       Opacity="0.8"
                                                       HorizontalAlignment="Right"
                                                       Margin="0,6,0,0"/>
                                        </StackPanel>

                                        <!-- Status -->
                                        <Border Grid.Column="2"
                                                Background="#FF132735"
                                                CornerRadius="12"
                                                Padding="10,6"
                                                VerticalAlignment="Top">
                                            <TextBlock Text="{Binding StatusText}"
                                                       Foreground="#FFA8E5FC"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"/>
                                        </Border>
                                    </Grid>

                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </ScrollViewer>

            </Grid>
        </Border>
    </Grid>
</UserControl>
