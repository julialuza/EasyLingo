<UserControl x:Class="EasyLingo.Views.TopBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="50"
             Background="#FF030A13">

    <Grid Margin="20,14,20,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="251*"/>
            <ColumnDefinition Width="125*"/>
            <ColumnDefinition Width="73*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="4"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">

            <ComboBox x:Name="LanguageComboBox" Width="110" Height="20" BorderBrush="#FF132735" Foreground="#FF0C0707" Background="#FF132735" SelectedIndex="0" SelectionChanged="LanguageComboBox_SelectionChanged">
                <ComboBoxItem Content="Angielski"/>
                <ComboBoxItem Content="Niemiecki"/>
            </ComboBox>

            <Ellipse Width="20"
                     Height="20"
                     Fill="#2E8BC0" Margin="50,0,6,0"/>

            <TextBlock Text="{Binding UserName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Margin="10,0,10,0"
                       VerticalAlignment="Center"
                       Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
