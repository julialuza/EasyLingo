<UserControl x:Class="EasyLingo.Views.TypingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FF030912"/>
            <GradientStop Color="#FF132735" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="← WRÓĆ"
                    Command="{Binding BackCommand}"
                    Width="110" Height="35"
                    Background="#FF0D1E29" Foreground="#FFA8E5FC"
                    BorderBrush="{x:Null}" FontFamily="Bahnschrift"/>

            <TextBlock Grid.Column="1"
                       Text="Wpisywanie"
                       Foreground="#FFA8E5FC"
                       FontSize="26"
                       FontWeight="SemiBold"
                       Margin="16,0,0,0"
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Right">
                <TextBlock Text="{Binding ProgressText}"
                           Foreground="#FFA8E5FC"
                           FontSize="14"
                           HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding ScoreText}"
                           Foreground="#FFA8E5FC"
                           FontSize="14"
                           HorizontalAlignment="Right"/>
            </StackPanel>
        </Grid>

        <!-- Centered card -->
        <Grid Grid.Row="1">
            <Border Background="#FF0D1E29"
                    CornerRadius="22"
                    Padding="28"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MaxWidth="760"
                    Width="760">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Prompt -->
                    <TextBlock Grid.Row="0"
                               Text="{Binding Prompt}"
                               Foreground="White"
                               FontSize="28"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               Margin="0,6,0,18"/>

                    <!-- Input -->
                    <TextBox Grid.Row="1"
                             Text="{Binding Answer, UpdateSourceTrigger=PropertyChanged}"
                             Background="#FF132735"
                             Foreground="White"
                             BorderBrush="#FF1F3B4F"
                             BorderThickness="1"
                             Height="56"
                             Padding="16,10"
                             FontSize="20"
                             FontFamily="Bahnschrift"
                             HorizontalContentAlignment="Center"
                             VerticalContentAlignment="Center">
                        <TextBox.InputBindings>
                            <!-- Enter = sprawdź -->
                            <KeyBinding Key="Enter" Command="{Binding PrimaryEnterCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <!-- Hint / feedback -->
                    <TextBlock Grid.Row="2"
                               Text="{Binding Feedback}"
                               Foreground="White"
                               Opacity="0.9"
                               FontSize="16"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Margin="0,14,0,0"/>

                    <!-- Buttons -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,22,0,0">
                        <Button Content="SPRAWDŹ"
                                Command="{Binding CheckCommand}"
                                Width="170" Height="44"
                                Margin="0,0,12,0"
                                Background="#FF132735"
                                Foreground="White"
                                BorderBrush="{x:Null}"
                                FontFamily="Bahnschrift"
                                IsEnabled="{Binding CanCheck}"/>

                        <Button Content="{Binding NextButtonText}"
                                Command="{Binding NextCommand}"
                                Width="170" Height="44"
                                Background="#FF0D1E29"
                                Foreground="#FFA8E5FC"
                                BorderBrush="#FF1F3B4F"
                                BorderThickness="1"
                                FontFamily="Bahnschrift"/>
                    </StackPanel>

                    <!-- End screen actions -->
                    <StackPanel Grid.Row="4"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="0,16,0,0"
                                Visibility="{Binding EndActionsVisible}">
                        <Button Content="Powtórz błędne"
                                Command="{Binding RepeatWrongCommand}"
                                Width="190" Height="38"
                                Margin="0,0,12,0"
                                Background="#FF132735"
                                Foreground="White"
                                BorderBrush="{x:Null}"
                                FontFamily="Bahnschrift"/>

                        <Button Content="Powtórz wszystkie"
                                Command="{Binding RepeatAllCommand}"
                                Width="190" Height="38"
                                Background="#FF132735"
                                Foreground="White"
                                BorderBrush="{x:Null}"
                                FontFamily="Bahnschrift"/>
                    </StackPanel>

                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
